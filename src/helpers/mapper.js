// const mapper = (item) => {
//   const baseUrl = "";
//   const mapImage = (image) => ({
//     url: `${baseUrl}${image.fileName}_full.jpg`,
//     thumbnailUrl: `${baseUrl}${image.fileName}_thumb.jpg`,
//   });

//   return {
//     ...item,
//     image: item.image.map(mapImage),
//   };
// };

// module.exports = mapper;
